import Address from '../containers/Address';
import Home from '../containers/Home';

const routes = [
  {
    path: '/',
    component: Home,
  },
  {
    path: '/address',
    component: Address,
  },
];

export default routes;
